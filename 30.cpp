#include<stdio.h>

	int main() {
		float a, b;
		scanf("%lf%lf", &a, &b);
		if (a != 0 && b != 0) {
			printf("a va b khong bang 0");
		} else {
			printf("a va b bang 0");
		}
	
	
	
	
		return 0;	
	}
