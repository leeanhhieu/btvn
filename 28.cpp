#include<stdio.h>

	int main() {
		int n;
		scanf("%d", &n);
	
		if (n>0) {
			printf("n la so duong");
		}
		if (n<0) {
			printf("n la so am");
			
		}
		if (n==0) {
			printf("n bang 0");
			
		}
		return 0;
	}
	
